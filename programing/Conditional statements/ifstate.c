// simple if statements
#include<stdio.h>
int main(){
    int number;
    printf("Enter number:");
    scanf("%d",&number);
    if(number>10){ // 
        printf("Number is positive");
    }

    printf("\nNumber is not valid accoring to the condition");
    return 0;
}